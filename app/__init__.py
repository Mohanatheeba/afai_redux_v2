"""afai-redux — document ingestion service.

Parses documents into structured chunks with rich metadata and provenance.
Embedding and vector storage are handled by the calling system (AeroChat).
"""

__version__ = "0.1.0"
